
val n = 10000;
val a = rand(n,n);
val b = rand(n,n);
val c = zeros(n,n);

tic
var i = 0;
while (i < n) {
  var j = 0;
  while (j < n) {
     c(j,i) = a(j,i) + b(j,i);
     j +=1;
  }
  i += 1;
}
val t1 = toc;

c ~ a+b;

val t2 = toc - t1;

println("%f,%f" format (t1,t2));

