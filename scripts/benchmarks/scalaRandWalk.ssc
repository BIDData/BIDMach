
import java.util.Random

val randgen = new Random

def rw(n:Int) = {
    var i = 0;
    var sum = 0f;
    while (i < n) {
          sum += randgen.nextFloat
          i += 1;
    }
    sum
}
    
def fib(n:Long):Long = {
    if (n <= 2) 1
    else fib(n-2) + fib(n-1)
}
    